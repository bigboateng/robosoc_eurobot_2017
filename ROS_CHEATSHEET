rospack = ros + pack(age)
roscd = ros + cd
rosls = ros + ls


// Creating a new package (You probably wont ever have to do this but its good to know
cd ~/catkin_ws/src
catkin_create_pkg <package_name> [depend1] [depend2] [depend3]
